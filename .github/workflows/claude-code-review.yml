name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch:
  
permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write
  actions: read
  checks: read
  statuses: read

jobs:
  claude-review:
    uses: liquibase/sayali-test/.github/workflows/claude-code.yml@main
    secrets: inherit
